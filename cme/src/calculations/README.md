# Calculations (Legacy Placeholder)

This pre-existing placeholder is preserved from repository creation. New shared calculations should be organized by domain under `src/core/`; construction-object-specific calculations belong with their module in `src/tools/`. This folder should not receive production code without an explicit architecture decision.

