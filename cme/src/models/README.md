# Models (Legacy Placeholder)

This pre-existing placeholder is preserved from repository creation. Shared document and construction-object contracts now belong under `src/core/`; concrete construction models belong in `src/tools/`. This folder should not receive production code without an explicit architecture decision.

