# Core

Shared engine services and domain primitives used by multiple construction objects live here. Core must remain independent of UI frameworks, application routes, and DCR Sales Hub state. Object-specific rules belong in `src/tools/`, not here.

