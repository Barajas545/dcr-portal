# Geometry

Framework-independent geometric primitives, operations, tolerances, and spatial algorithms shared across CME. Geometry should express precise, testable data and must not contain tool-specific construction policy.

