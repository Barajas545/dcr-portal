// ---------------------------------------------------------------------------
// DCR Portal configuration (safe to be public — contains NO secrets).
//
// Set API_BASE to your Vercel deployment URL (the project that hosts /api).
// Example: "https://share-point-api.vercel.app"
// Do NOT put a trailing slash.
// ---------------------------------------------------------------------------
window.DCR_CONFIG = {
  API_BASE: "https://share-point-api.vercel.app",
  COMPANY_NAME: "DCR Framing",
};
